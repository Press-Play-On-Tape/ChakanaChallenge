
#pragma once

#include <Arduboy2.h>
#include "../utils/Constants.h"
#include "../utils/Enums.h"

struct Game {

    private:
        // uint8_t numOfPlayers;
        // uint8_t dealerIdx;
        // uint16_t prizePool;
        // uint16_t prizePoolThisRound;
        // uint16_t defaultBet;
        // uint8_t winnerIdx;
        // uint8_t hideCards = 0;
        // uint8_t smallBlindIdx = 0;
        // uint8_t largeBlindIdx = 0;
        // uint8_t betRound;
        // uint8_t pickUpCardIdx;

    public:

        // uint8_t getNumberOfPlayers()                    { return this->numOfPlayers; }
        // uint8_t getDealerIdx()                          { return this->dealerIdx; }
        // uint8_t getWinnerIdx()                          { return this->winnerIdx; }
        // uint16_t getPrizePool()                         { return this->prizePool; }
        // uint16_t getPrizePoolThisRound()                { return this->prizePoolThisRound; }
        // uint8_t getHideCards()                          { return this->hideCards; }
        // uint8_t getSmallBlindIdx()                      { return this->smallBlindIdx; }
        // uint8_t getLargeBlindIdx()                      { return this->largeBlindIdx; }
        // uint8_t getBetRound()                           { return this->betRound; }


        // void setNumberOfPlayers(uint8_t val)            { this->numOfPlayers = val; }
        // void setDealerIdx(uint8_t val)                  { this->dealerIdx = val; }
        // void setWinnerIdx(uint8_t val)                  { this->winnerIdx = val; }
        // void setPrizePool(uint16_t val)                 { this->prizePool = val; }
        // void setPrizePoolThisRound(uint16_t val)        { this->prizePoolThisRound = val; }
        // void setHideCards(uint8_t val)                  { this->hideCards = val; }
        // void setSmallBlindIdx(uint8_t val)              { this->smallBlindIdx = val; }
        // void setLargeBlindIdx(uint8_t val)              { this->largeBlindIdx = val; }
        // void setBetRound(uint8_t val)                   { this->betRound = val; }


};
